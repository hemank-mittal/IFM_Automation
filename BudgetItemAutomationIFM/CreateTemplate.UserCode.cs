///////////////////////////////////////////////////////////////////////////////
//
// This file was automatically generated by RANOREX.
// Your custom recording code should go in this file.
// The designer will only add methods to this file, so your custom code won't be overwritten.
// http://www.ranorex.com
//
///////////////////////////////////////////////////////////////////////////////

using System;
using System.Collections.Generic;
using System.Text;
using System.Text.RegularExpressions;
using System.Drawing;
using System.Threading;
using WinForms = System.Windows.Forms;

using Ranorex;
using Ranorex.Core;
using Ranorex.Core.Repository;
using Ranorex.Core.Testing;

namespace BudgetItemAutomationIFM
{
    public partial class CreateTemplate
    {
        /// <summary>
        /// This method gets called right after the recording has been started.
        /// It can be used to execute recording specific initialization code.
        /// </summary>
        private void Init()
        {
            // Your recording specific initialization code goes here.
        }

        public void Mouse_Click_FirstInstance(string tabName)
        {
        	RepoItemInfo divToClick = null;
        	
        	if (tabName.ToLower().Contains("budgetitem"))
        	{
        		divToClick = repo.ApplicationUnderTest.Content1.itemDropdown_FirstInstanceInfo;
        	}
        	else if (tabName.ToLower().Contains("closeout") || tabName.ToLower().Contains("close_out"))
        	{
        		divToClick = repo.ApplicationUnderTest.CloseOutItemDropdown_FirstInstanceInfo;
        	}
        	        	
            Report.Log(ReportLevel.Info, "Mouse", "Mouse Left Click item 'divtagInfo' at Center.", divToClick);
            divToClick.FindAdapter<DivTag>().Click();
        }

        public void Mouse_Click_SecondInstance(string tabName)
        {
        	RepoItemInfo divToClick = null;
        	
        	if (tabName.ToLower().Contains("budgetitem"))
        	{
        		divToClick = repo.ApplicationUnderTest.Content1.itemDropdown_SecondInstanceInfo;
        	}
        	else if (tabName.ToLower().Contains("closeout") || tabName.ToLower().Contains("close_out"))
        	{
        		divToClick = repo.ApplicationUnderTest.CloseOutItemDropdown_SecondInstanceInfo;
        	}
            Report.Log(ReportLevel.Info, "Mouse", "Mouse Left Click item 'divtagInfo' at Center.", divToClick);
            divToClick.FindAdapter<DivTag>().Click();
        }

    }
}
